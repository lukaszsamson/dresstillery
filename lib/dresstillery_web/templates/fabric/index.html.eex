<h2>Listing Fabrics</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Code</th>
      <th>Available</th>
      <th>Hidden</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for fabric <- @fabrics do %>
    <tr>
      <td><%= fabric.name %></td>
      <td><%= fabric.code %></td>
      <td><%= fabric.available %></td>
      <td><%= fabric.hidden %></td>

      <td class="text-right">
        <span><%= link "Show", to: Routes.fabric_path(@conn, :show, fabric), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Edit", to: Routes.fabric_path(@conn, :edit, fabric), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Delete", to: Routes.fabric_path(@conn, :delete, fabric), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Fabric", to: Routes.fabric_path(@conn, :new) %></span>
